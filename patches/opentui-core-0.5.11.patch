diff --git a/chunk-bun-37s3zwb6.js b/chunk-bun-37s3zwb6.js
--- a/chunk-bun-37s3zwb6.js
+++ b/chunk-bun-37s3zwb6.js
@@ -13588,7 +13588,7 @@
 var targetLibPath;
 var targetLibError;
 try {
-  targetLibPath = await resolveNativeLibraryPath();
+  targetLibPath = await materializeLibrary(await resolveNativeLibraryPath());
   if (isBunfsPath(targetLibPath)) {
     targetLibPath = targetLibPath.replace("../", "");
   }
@@ -13732,7 +13732,7 @@
   if (!resolvedLibPath) {
     throw targetLibError ?? new Error(`OpenTUI is not supported on the current platform: ${process.platform}-${process.arch}`);
   }
-  const rawSymbols = dlopen(resolvedLibPath, {
+  const rawSymbols = dlopenLazy(resolvedLibPath, {
     setLogCallback: {
       args: ["ptr"],
       returns: "void"
@@ -18790,3 +18790,83 @@
 
 //# debugId=C2AA7ABE4C3E076964756E2164756E21
 //# sourceMappingURL=chunk-bun-37s3zwb6.js.map
+
+function dlopenLazy(path, definitions, load = dlopen) {
+  if (String(path).includes("$bunfs") || /^B:[\\/]~BUN/i.test(String(path)))
+    return load(path, definitions);
+  const entries = Object.entries(definitions), first = entries[0];
+  if (!first)
+    return load(path, definitions);
+  const anchor = load(path, { [first[0]]: first[1] }), libraries = [anchor], symbols = Object.create(null);
+  let closed = !1;
+  for (const [name, definition] of entries) {
+    if (name === first[0]) {
+      Object.defineProperty(symbols, name, {
+        value: anchor.symbols[name],
+        enumerable: !0,
+        writable: !0,
+        configurable: !0
+      });
+      continue;
+    }
+    Object.defineProperty(symbols, name, {
+      enumerable: !0,
+      configurable: !0,
+      get() {
+        if (closed)
+          throw Error("Cannot resolve a symbol after library has closed");
+        const library = load(path, { [name]: definition });
+        libraries.push(library);
+        const symbol = library.symbols[name];
+        Object.defineProperty(symbols, name, { value: symbol, enumerable: !0, configurable: !0, writable: !0 });
+        return symbol;
+      },
+      set(value) {
+        Object.defineProperty(symbols, name, { value, enumerable: !0, configurable: !0, writable: !0 });
+      }
+    });
+  }
+  return {
+    symbols,
+    createCallback(callback, definition) {
+      if (closed)
+        throw Error("Cannot create a callback after library has closed");
+      return anchor.createCallback(callback, definition);
+    },
+    close() {
+      if (closed)
+        return;
+      closed = !0;
+      const failures = [];
+      for (let index = libraries.length - 1;index >= 0; index--)
+        try {
+          libraries[index].close();
+        } catch (error) {
+          failures.push(error);
+        }
+      libraries.length = 0;
+      if (failures.length === 1)
+        throw failures[0];
+      if (failures.length > 1)
+        throw AggregateError(failures, "Failed to close native bindings");
+    }
+  };
+}
+
+async function materializeLibrary(path) {
+  if (!path.includes("$bunfs") || /^B:[\\/]~BUN/i.test(path))
+    return path;
+  path = path.replace("../", "");
+  const { mkdtemp, readFile, writeFile, rm } = await import("node:fs/promises"), { rmSync } = await import("node:fs"), { tmpdir } = await import("node:os"), { basename, join } = await import("node:path"), directory = await mkdtemp(join(tmpdir(), "opentui-native-"));
+  try {
+    const destination = join(directory, basename(path));
+    await writeFile(destination, await readFile(path), { flag: "wx", mode: 384 });
+    process.once("exit", () => {
+      rmSync(directory, { recursive: !0, force: !0 });
+    });
+    return destination;
+  } catch (error) {
+    await rm(directory, { recursive: !0, force: !0 });
+    throw error;
+  }
+}
diff --git a/chunk-node-70eg2nhg.js b/chunk-node-70eg2nhg.js
--- a/chunk-node-70eg2nhg.js
+++ b/chunk-node-70eg2nhg.js
@@ -13570,7 +13570,7 @@
 var targetLibPath;
 var targetLibError;
 try {
-  targetLibPath = await resolveNativeLibraryPath();
+  targetLibPath = await materializeLibrary(await resolveNativeLibraryPath());
   if (isBunfsPath(targetLibPath)) {
     targetLibPath = targetLibPath.replace("../", "");
   }
@@ -13714,7 +13714,7 @@
   if (!resolvedLibPath) {
     throw targetLibError ?? new Error(`OpenTUI is not supported on the current platform: ${process.platform}-${process.arch}`);
   }
-  const rawSymbols = dlopen(resolvedLibPath, {
+  const rawSymbols = dlopenLazy(resolvedLibPath, {
     setLogCallback: {
       args: ["ptr"],
       returns: "void"
@@ -18772,3 +18772,83 @@
 
 //# debugId=C8F70BCDECDD4B1164756E2164756E21
 //# sourceMappingURL=chunk-node-70eg2nhg.js.map
+
+function dlopenLazy(path, definitions, load = dlopen) {
+  if (String(path).includes("$bunfs") || /^B:[\\/]~BUN/i.test(String(path)))
+    return load(path, definitions);
+  const entries = Object.entries(definitions), first = entries[0];
+  if (!first)
+    return load(path, definitions);
+  const anchor = load(path, { [first[0]]: first[1] }), libraries = [anchor], symbols = Object.create(null);
+  let closed = !1;
+  for (const [name, definition] of entries) {
+    if (name === first[0]) {
+      Object.defineProperty(symbols, name, {
+        value: anchor.symbols[name],
+        enumerable: !0,
+        writable: !0,
+        configurable: !0
+      });
+      continue;
+    }
+    Object.defineProperty(symbols, name, {
+      enumerable: !0,
+      configurable: !0,
+      get() {
+        if (closed)
+          throw Error("Cannot resolve a symbol after library has closed");
+        const library = load(path, { [name]: definition });
+        libraries.push(library);
+        const symbol = library.symbols[name];
+        Object.defineProperty(symbols, name, { value: symbol, enumerable: !0, configurable: !0, writable: !0 });
+        return symbol;
+      },
+      set(value) {
+        Object.defineProperty(symbols, name, { value, enumerable: !0, configurable: !0, writable: !0 });
+      }
+    });
+  }
+  return {
+    symbols,
+    createCallback(callback, definition) {
+      if (closed)
+        throw Error("Cannot create a callback after library has closed");
+      return anchor.createCallback(callback, definition);
+    },
+    close() {
+      if (closed)
+        return;
+      closed = !0;
+      const failures = [];
+      for (let index = libraries.length - 1;index >= 0; index--)
+        try {
+          libraries[index].close();
+        } catch (error) {
+          failures.push(error);
+        }
+      libraries.length = 0;
+      if (failures.length === 1)
+        throw failures[0];
+      if (failures.length > 1)
+        throw AggregateError(failures, "Failed to close native bindings");
+    }
+  };
+}
+
+async function materializeLibrary(path) {
+  if (!path.includes("$bunfs") || /^B:[\\/]~BUN/i.test(path))
+    return path;
+  path = path.replace("../", "");
+  const { mkdtemp, readFile, writeFile, rm } = await import("node:fs/promises"), { rmSync } = await import("node:fs"), { tmpdir } = await import("node:os"), { basename, join } = await import("node:path"), directory = await mkdtemp(join(tmpdir(), "opentui-native-"));
+  try {
+    const destination = join(directory, basename(path));
+    await writeFile(destination, await readFile(path), { flag: "wx", mode: 384 });
+    process.once("exit", () => {
+      rmSync(directory, { recursive: !0, force: !0 });
+    });
+    return destination;
+  } catch (error) {
+    await rm(directory, { recursive: !0, force: !0 });
+    throw error;
+  }
+}
diff --git a/renderer-entry.bun.js b/renderer-entry.bun.js
new file mode 100644
--- /dev/null
+++ b/renderer-entry.bun.js
@@ -0,0 +1,8 @@
+export { CliRenderer2 as CliRenderer } from "./chunk-bun-bwjmgnxw.js";
+export { MouseEvent2 as MouseEvent } from "./chunk-bun-bwjmgnxw.js";
+export { RGBA2 as RGBA } from "./chunk-bun-37s3zwb6.js";
+export { Renderable2 as Renderable } from "./chunk-bun-bwjmgnxw.js";
+export { StdinParser2 as StdinParser } from "./chunk-bun-37s3zwb6.js";
+export { TextAttributes2 as TextAttributes } from "./chunk-bun-37s3zwb6.js";
+export { createCliRenderer2 as createCliRenderer } from "./chunk-bun-bwjmgnxw.js";
+export { parseColor2 as parseColor } from "./chunk-bun-37s3zwb6.js";
diff --git a/renderer-entry.js b/renderer-entry.js
new file mode 100644
--- /dev/null
+++ b/renderer-entry.js
@@ -0,0 +1,8 @@
+export { CliRenderer2 as CliRenderer } from "./chunk-node-54dhb2fr.js";
+export { MouseEvent2 as MouseEvent } from "./chunk-node-54dhb2fr.js";
+export { RGBA2 as RGBA } from "./chunk-node-70eg2nhg.js";
+export { Renderable2 as Renderable } from "./chunk-node-54dhb2fr.js";
+export { StdinParser2 as StdinParser } from "./chunk-node-70eg2nhg.js";
+export { TextAttributes2 as TextAttributes } from "./chunk-node-70eg2nhg.js";
+export { createCliRenderer2 as createCliRenderer } from "./chunk-node-54dhb2fr.js";
+export { parseColor2 as parseColor } from "./chunk-node-70eg2nhg.js";
diff --git a/renderer-entry.d.ts b/renderer-entry.d.ts
new file mode 100644
--- /dev/null
+++ b/renderer-entry.d.ts
@@ -0,0 +1,10 @@
+/** Renderer primitives without the full widget, Markdown and audio entrypoint.
+ * All exports share identity and native ownership with the main package.
+ */
+export { createCliRenderer, CliRenderer, MouseEvent, type CliRendererConfig } from "./renderer.js"
+export { Renderable, type RenderableOptions } from "./Renderable.js"
+export { RGBA, parseColor } from "./lib/RGBA.js"
+export { TextAttributes, type RenderContext } from "./types.js"
+export { StdinParser, type StdinEvent } from "./lib/stdin-parser.js"
+export type { KeyEvent } from "./lib/KeyHandler.js"
+export type { OptimizedBuffer } from "./buffer.js"
diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -66,6 +66,12 @@
     "./node-assets": {
       "types": "./node-assets.d.ts",
       "import": "./node-assets.js"
+    },
+    "./renderer": {
+      "types": "./renderer-entry.d.ts",
+      "bun": "./renderer-entry.bun.js",
+      "node": "./renderer-entry.js",
+      "import": "./renderer-entry.js"
     }
   },
   "dependencies": {
@@ -88,4 +94,4 @@
     "@opentui/core-linux-x64-musl": "0.5.11",
     "@opentui/core-linux-arm64-musl": "0.5.11"
   }
-}
\ No newline at end of file
+}
